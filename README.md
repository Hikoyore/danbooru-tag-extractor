# Danbooru Tag Extractor

Простой набор скриптов для работы с тегами изображений с Danbooru.

## 📦 Состав инструментов

### 1. Danbooru Tag Extractor (`danbooru-tag-extractor.py`)
Скрипт для получения тегов изображения с Danbooru по ссылке.

**Возможности:**
- Извлечение ID поста из различных форматов URL Danbooru
- Получение тегов через официальное API
- Автоматическая очистка тегов (замена `_` на пробелы)
- Сохранение тегов в текстовый файл
- Поддержка поиска как по ID, так и по MD5 хешу

**Использование:**
```bash
python danbooru_tags.py
```
Затем вставьте ссылку на изображение Danbooru (например: `https://danbooru.donmai.us/posts/123456`)

**Формат выходного файла:** `tags_{ID}_clean.txt`
```
тег1, тег2, тег3, ...
Всего тегов: 28
```

### 2. Объединение тегов (`merge.py`)
Скрипт для объединения всех файлов с тегами в один общий файл.

**Возможности:**
- Автоматический поиск всех файлов `tags_*_clean.txt`
- Объединение тегов из всех файлов
- Сохранение структуры (каждый набор тегов с новой строки и с отступом)

**Использование:**
```bash
python merge.py
```

**Формат выходного файла:** `all_tags_combined.txt`
```
теги из первого файла

теги из второго файла

теги из третьего файла
...
```

## 📁 Структура файлов

После работы скриптов создаются следующие файлы:
- `tags_{ID}_clean.txt` - теги для конкретного изображения
- `all_tags_combined.txt` - объединенные теги из всех файлов

## 🔧 Требования

- Python 3.6 или выше`

## 🚀 Быстрый старт

1. Скачайте оба скрипта в одну папку
3. Запустите `danbooru_tags.py` для получения тегов
4. Накопив несколько файлов, запустите `merge.py` для их объединения

## 📝 Примеры

**Получение тегов:**
```
==================================================
Danbooru Tag Extractor
==================================================
Вставьте ссылку на изображение Danbooru

🔗 Ссылка: https://danbooru.donmai.us/posts/123456
🔄 Получаю теги...
✅ Сохранено 28 тегов в tags_123456_clean.txt
```

**Объединение тегов:**
```
==================================================
Объединение всех тегов в один файл
==================================================
Найдено файлов: 183

✅ tags_123456_clean.txt
✅ tags_123457_clean.txt
✅ tags_123458_clean.txt
...

==================================================
✅ Готово! Обработано файлов: 183
📄 Результат сохранен в: all_tags_combined.txt
```

## ⚠️ Примечания

- Скрипты должны находиться в той же папке, что и файлы с тегами
- Для работы `danbooru-tags-extractor.py` требуется интернет-соединение
- API Danbooru может иметь ограничения на количество запросов
- Файлы с тегами сохраняются в кодировке UTF-8
