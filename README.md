Мне для `Stable Diffusion` потребовались теги, а лучшим сайтом является Danbooru. Так что "брать" теги будем в основном на нём, но учтены и остальные сайты.

# Universal Tag Extractor

Набор скриптов для извлечения тегов изображений с различных борд и объединения их в один файл.  
Основной скрипт (`tag_extractor.py`) получает теги по ссылке на пост, для Yande.re и Gelbooru выполняется автоматический поиск того же изображения на Danbooru.  
Дополнительный скрипт (`merge.py`) собирает все полученные файлы с тегами в общий файл.

## 📦 Состав

### 1. `tag_extractor.py` — получение тегов
- Прямая поддержка **Danbooru**, **Aibooru**, **Konachan**.
- Для **Yande.re** и **Gelbooru**:
  - Извлечение URL изображения и MD5 через API источника.
  - Поиск поста на Danbooru (сначала по MD5, при неудаче — через сервис IQDB).
  - Получение тегов с найденного поста Danbooru.
- Сохранение результата в файл `tags_домен_ID_clean.txt` с указанием исходной ссылки и ссылки на Danbooru (если применимо).

### 2. `merge.py` — объединение всех тегов
- Автоматически находит все файлы `tags_*_clean.txt` в текущей папке.
- Извлекает первую строку (строку с тегами) из каждого файла.
- Сохраняет все наборы тегов в один файл `all_tags_combined.txt`, разделяя их пустыми строками.

## 🔧 Требования

- Python 3.6 или выше
