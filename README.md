# Tag Extractor

Простой набор скриптов для получения и обработки тегов изображений с сайтов Danbooru, Aibooru и Konachan.

## 📦 Состав инструментов

### 1. Tag Extractor (`tag_extractor.py`)
Скрипт для получения тегов изображения по ссылке на пост или прямое изображение.

**Поддерживаемые сайты:**
- `danbooru.donmai.us`
- `aibooru.online`
- `konachan.com`
- `konachan.net`

**Возможности:**
- Автоматическое извлечение ID поста или MD5 хеша из URL (поддерживаются различные форматы ссылок)
- Получение тегов через официальное API каждого сайта
- Замена подчёркиваний `_` на пробелы в тегах
- Сохранение тегов в текстовый файл с именем, содержащим домен и ID
- Поиск как по числовому ID, так и по MD5

**Использование:**
python tag_extractor.py

### 2. **Объединение тегов (`merge.py`)**
Скрипт для объединения всех файлов с тегами в один общий файл.

Возможности:
- Автоматический поиск всех файлов tags_*_clean.txt в текущей папке
- Извлечение первой строки (строки с тегами) из каждого файла
- Сохранение всех наборов тегов в один файл с разделением пустыми строками

**Использование:**
python merge.py
